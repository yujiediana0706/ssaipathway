# Pathway 开发历程分享

> 本文档记录了 Pathway 项目从 0 到 1 的完整开发过程，希望能为组员提供参考，了解 AI 辅助开发的工作流程和最佳实践。

---

## 🎯 第一章：项目启动

### 第一步：定义需求

**我做了什么：**
1. 撰写了一份详细的 PRD（产品需求文档）
2. 定义了目标用户、核心功能、技术方案
3. 明确了 MVP（最小可行产品）的范围

**PRD 核心内容：**
- 产品定位：AI 驱动的职业转型平台
- 核心功能：AI 诊断、岗位模拟、Coach 双轨
- 技术栈：Next.js + Supabase + GLM AI

**给大家的启示：**
> 在开始写代码之前，先花时间想清楚「做什么」和「为什么做」。一份好的 PRD 能让 AI 更准确地理解你的需求。

---

## 🚀 第二章：快速原型

### 第二步：让 AI 生成第一个版本

**我做了什么：**
1. 将 PRD 上传给 AI
2. 要求 AI 基于 PRD 创建项目骨架
3. 让 AI 按模块逐个实现功能

**实际过程：**
```
Day 1:
├── 创建项目结构（frontend + backend）
├── 搭建基础路由（首页、登录、Dashboard）
├── 实现 AI 对话页面（/explore）
└── 生成第一个可运行的 prototype

Day 2:
├── 增加诊断报告页面（/report）
├── 实现岗位模拟器（/simulator）
├── 添加 Coach 双轨系统（/coach）
└── 完成所有核心页面
```

**关键产出：**
- 第一个能跑起来的 Demo
- 基本的页面路由和交互
- 本地存储（localStorage）暂存数据

**给大家的启示：**
> 先用 AI 生成一个「能跑的版本」，再逐步完善。不要追求一次就做到完美，快速迭代才是 AI 开发的正确姿势。

---

## 🔧 第三章：迭代优化

### 第三步：用户反馈驱动改进

**遇到的问题和解决方案：**

#### 问题 1：主题配色不满意
- **现象**：初始版本用的是黑色主题，感觉太沉闷
- **改进**：改为深藏青配色，加入科技感的青蓝色点缀
- **结果**：整体风格更现代、专业

#### 问题 2：Dashboard 数据逻辑错误
- **现象**：「待掌握技能」显示的是用户自评技能，不是 AI 推荐技能
- **根因**：数据源用错了（`user.skills` vs `report.skillsToAcquire`）
- **修复**：创建 `reportStore.ts` 保存报告数据，Dashboard 从报告读取
- **结果**：Dashboard 正确展示 AI 推荐的转型方向

#### 问题 3：交互体验优化
- **调整**：首页文案改为通用欢迎语（不预设用户名）
- **调整**：添加语音输入功能（Web Speech API）
- **调整**：优化移动端适配

**给大家的启示：**
> AI 生成的第一版难免有逻辑错误。通过实际使用和用户反馈，逐个问题修复，产品就会越来越好。

---

## 🗄️ 第四章：数据库升级

### 第四步：从 localStorage 迁移到 Supabase

**为什么需要数据库？**
- 之前用 localStorage 存储数据，用户换设备就丢失
- 需要支持多用户、多设备访问
- 需要更可靠的数据持久化

**Supabase 搭建流程：**

#### Step 1：创建 Supabase 项目
1. 打开 [supabase.com](https://supabase.com) 注册
2. 点击「New Project」创建项目
3. 设置项目名和密码

#### Step 2：设计数据库结构
- 让 AI 根据业务需求设计 7 张核心表
- 定义表关系、字段类型、RLS 安全策略
- 生成 `schema.sql` 建表语句

#### Step 3：执行建表
- 打开 Supabase SQL Editor
- 粘贴 `schema.sql` 内容执行
- 导入初始数据（教练 Mock 数据）

#### Step 4：集成到前端
- 创建 `lib/supabase.ts`：Supabase 客户端
- 创建 `lib/db.ts`：数据访问层
- 实现 localStorage + Supabase 双层同步

**遇到的坑：**
- PostgreSQL 保留关键字问题（`role`、`user` 不能直接作列名）
- 解决方案：用双引号包裹关键字，或改名

**给大家的启示：**
> MVP 阶段可以先用 localStorage 快速验证，确认产品方向后再升级到数据库。Supabase 免费、简单，非常适合创业项目。

---

## 🚢 第五章：部署尝试

### 第五步：尝试上线 Vercel

**部署流程：**
1. 将代码推送到 GitHub
2. 在 Vercel Dashboard 导入仓库
3. 配置环境变量（Supabase URL、Keys 等）
4. 点击 Deploy

**遇到的问题：**

#### 问题 1：Node.js 版本不兼容
- **现象**：Vercel 默认用 Node.js 24，Supabase SDK 不兼容
- **解决**：在 Vercel 设置中手动切换到 Node.js 22

#### 问题 2：Production Override 锁定
- **现象**：Vercel 的 Production Override 灰色，无法修改
- **解决**：保持现状，只要 Project Settings 是 22.x 即可

#### 问题 3：国内网络无法访问
- **现象**：Vercel URL 在公司/学校网络下超时
- **原因**：Vercel 服务器在国外，部分网络限制访问
- **当前方案**：本地开发 + git pull 同步

**给大家的启示：**
> 部署到 Vercel 不难，但国内访问是个常见问题。如果需要国内用户访问，通常需要购买域名 + CDN 加速，或者使用国内云服务（阿里云、腾讯云）。

---

## 📚 第六章：文档与协作

### 第六步：完善项目文档

**创建的文档：**
- `README.md`：项目介绍 + 上手指南
- `PROJECT_JOURNAL.md`：开发日志
- `DEVELOPMENT_JOURNEY.md`：本文档（开发历程分享）
- `backend/README.md`：后端数据库说明

**文档的重要性：**
- 方便新成员快速上手
- 记录技术决策的理由
- 便于后期维护和交接

**给大家的启示：**
> 好的项目离不开好的文档。建议每完成一个阶段，就更新一下 README 和日志。

---

## 🎯 我的开发方法论总结

### 1. 需求先行
- 在写代码之前，先写清楚「做什么」和「为什么做」
- PRD 越清晰，AI 生成的代码越准确

### 2. 快速原型
- 让 AI 先快速生成一个「能跑的版本」
- 不要追求一次完美，快速迭代

### 3. 用户导向
- 实际使用产品，找问题
- 根据用户反馈逐个修复
- 小步快跑，持续改进

### 4. 技术升级
- MVP 用简单方案（localStorage）验证
- 确认方向后再升级（Supabase）
- 选择适合创业的技术栈

### 5. 文档沉淀
- 每个阶段都记录下来
- 方便自己回顾和团队协作
- 好文档是项目可维护的基础

---

## 🛠️ AI 辅助开发工具推荐

| 工具 | 用途 |
|------|------|
| **Trae** | AI 编程助手，支持代码生成、调试、文档编写 |
| **GitHub Copilot** | 代码补全、单测生成 |
| **Supabase** | 后端 BaaS，快速搭建数据库和 API |
| **Vercel** | 前端部署平台，与 GitHub 集成 |

---

## 📖 延伸阅读

- [PROJECT_JOURNAL.md](./PROJECT_JOURNAL.md)：详细的开发日志
- [README.md](./README.md)：项目完整文档
- [backend/schema.sql](./backend/schema.sql)：数据库设计
- [backend/README.md](./backend/README.md)：后端技术说明

---

<p align="center">
  <strong>希望这份文档能帮助大家更好地理解 AI 辅助开发的流程 🚀</strong>
</p>
